CREATE TABLE animals (
    column1 INTEGER,
    id NUMERIC,
    organization VARCHAR(255),
	state VARCHAR(255),
    name VARCHAR(255),
    type VARCHAR(255),
    age VARCHAR(255),
    gender VARCHAR(255),
    size VARCHAR(255),
	url VARCHAR(500)
);

SELECT * FROM animals;